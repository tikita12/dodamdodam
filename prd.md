# PRD: 도담도담 자원봉사 일정관리

> **문서 버전**: v1.4 (최종 확정본)  
> **작성일**: 2026-08-27  
> **작성자**: PM (인터뷰 기반 작성)  
> **개발 범위**: MVP

---

## 1. 프로젝트 개요

### 1.1 배경
기존에 카카오 단톡방 공지 + 댓글 방식으로 운영하던 자원봉사 일정 조율 방식의 한계:
- 공지 글이 대화에 묻혀 찾기 어려움
- 누가 어떤 일정에 참여 가능한지 한눈에 파악 불가
- 참여 현황을 관리자가 수동으로 정리해야 하는 번거로움

### 1.2 목표
관리자가 일정을 등록하고, 자원봉사자들이 캘린더에서 직접 참여 신청/취소할 수 있는 **모바일 퍼스트 웹 애플리케이션** 제공

### 1.3 서비스명
```
도담도담 자원봉사 일정관리
```

---

## 2. 사용자 정의

### 2.1 관리자 (Admin)
| 항목 | 내용 |
|------|------|
| 인원 | 최대 2명 |
| 접속 방식 | 이메일 + 비밀번호 로그인 (Firebase Auth, 지정된 2개 이메일만 허용) |
| 주요 역할 | 일정 등록/수정/취소, 자원봉사자 명단 관리, 교육내용 목록 관리, 참여 확정 및 명단 직접 조정 |
| 사용 기기 | 스마트폰 또는 PC |

### 2.2 자원봉사자 (Volunteer)
| 항목 | 내용 |
|------|------|
| 인원 | 약 15명 |
| 접속 방식 | 관리자가 미리 등록한 이름 목록에서 자신의 이름 검색 및 선택 |
| 주요 역할 | 일정 확인(캘린더/목록), 참여 신청 및 취소, 내 신청 일정 확인 |
| 사용 기기 | 스마트폰 (모바일 퍼스트 최적화) |

> ⚠️ **보안 전제**: 이름 선택만으로 접속하므로, 소규모 신뢰 그룹(기존 봉사자 15인) 운영을 전제로 함.
> Firebase Auth 없이 접속하므로 Firestore Rules만으로 완전한 본인 인증은 불가능하며, 앱 클라이언트 로직과 Transaction으로 정상 사용 흐름의 동시성 및 데이터 무결성을 제어함.

---

## 3. 화면 구성 및 네비게이션

### 3.1 네비게이션 구조
**상단 헤더 + 뒤로가기 버튼** 방식 사용

```
┌──────────────────────────────┐
│ ← 도담도담 자원봉사   홍길동 ▾│  ← 상단 헤더 (모든 화면 상단 고정)
├──────────────────────────────┤
│                              │
│         화면 내용             │
│                              │
└──────────────────────────────┘
```

- **상단 헤더**: 모든 화면 상단에 고정
- **뒤로가기 (`←`)**: 메인/진입 화면을 제외한 하위 화면에서 좌측에 표시
- **자원봉사자 헤더 우측**: 현재 선택된 이름 표시 (`홍길동 ▾`), 클릭 시 이름 변경 모달 팝업
  - 이름 변경 시 `localStorage`가 갱신되며, 변경된 이름 기준으로 모든 화면(내 일정 등)이 즉시 전환됨
- **관리자 헤더 우측**: `관리자 모드` 배지 및 `로그아웃` 버튼 표시

### 3.2 화면 목록 및 URL

| 화면명 | URL | 접근 권한 | 주요 기능 |
|--------|-----|---------|----------|
| 진입 화면 | `/` | 전체 (미접속자) | 이름 검색 선택 입장 / 관리자 로그인 링크 |
| 메인 화면 | `/main` | 자원봉사자, 관리자 | 월별 캘린더 + 최근 등록된 일정 + 신청 가능 일정 |
| 일정 상세 화면 | `/schedule/:id` | 자원봉사자, 관리자 | 일정 정보, 카카오맵, 참여자 명단, 신청/취소 (관리자는 수정/취소 버튼) |
| 내 일정 화면 | `/my-schedules` | 자원봉사자 | 본인이 신청한 일정 모아보기 (진행중/다가오는/지난 일정) |
| 관리자 대시보드 | `/admin` | 관리자 | 일정 통합 관리(아코디언 명단 관리, 확정, 취소), 네비게이션 허브 |
| 관리자 일정 등록 | `/admin/schedule/new` | 관리자 | 일정 신규 등록 (카카오맵 연동) |
| 관리자 일정 수정 | `/admin/schedule/:id/edit` | 관리자 | 일정 정보 수정 (최신 정보 즉시 반영) |
| 관리자 봉사자 관리 | `/admin/volunteers` | 관리자 | 자원봉사자 이름 추가/삭제 |
| 관리자 교육내용 관리 | `/admin/subjects` | 관리자 | 교육 과목 항목 추가/삭제 |

---

## 4. 기능 요구사항

### 4.1 접속 화면 (진입 화면, `/`)

#### 자원봉사자 접속 흐름
1. **최초 방문**: `/` 화면으로 진입하여 검색 가능한 드롭다운에서 본인 이름 선택 후 "입장하기" 클릭
   - 선택된 `volunteerId`, `volunteerName`이 `localStorage`에 저장되고 `/main`으로 이동
2. **재방문**: `localStorage`에 이미 저장된 이름 정보가 있으면 진입 화면을 건너뛰고 **자동으로 `/main` 화면으로 이동**
3. **이름 변경**: 언제든 헤더 우측 이름을 클릭하여 다른 이름으로 재선택 가능

#### 관리자 로그인
- 진입 화면 하단에 작게 "관리자 로그인" 링크 배치
- 클릭 시 모달 또는 전용 폼에서 이메일/비밀번호 입력
- 지정된 관리자 이메일(2개) 로그인 성공 시 관리자 대시보드(`/admin`)로 자동 이동

---

### 4.2 메인 화면 (캘린더 & 목록 뷰, `/main`)

레이아웃은 상단부터 3개 섹션으로 구성:

```
┌─────────────────────────┐
│      월별 캘린더         │  ← Section 1
├─────────────────────────┤
│   🆕 최근 등록된 일정    │  ← Section 2
├─────────────────────────┤
│   📋 신청 가능한 일정    │  ← Section 3
└─────────────────────────┘
```

#### Section 1: 월별 캘린더
- **월 이동**: 이전 달 / 다음 달 이동 버튼 및 오늘 달 바로가기
- **일정 표시**: 날짜 칸 안에 `학교명 | n/n명 | 교육내용` 칩 표시 (예: `봉림초 2/3명 도박예방`)
- **다중 일정 처리**: 같은 날 일정이 2개 이상이면 첫 번째 일정 칩만 표시하고 하단에 `+n개 더` 표시
  - `+n개 더` 클릭 시 해당 날짜의 전체 일정 목록을 띄워주는 팝업 모달 제공
- **지난 일정** (`endAt < 현재시각`): 흑백(그레이스케일) 필터 적용 (클릭하여 조회 가능)
- **취소된 일정** (`status == "cancelled"`): 회색 톤 + "취소됨" 텍스트와 함께 표시
- **오늘 날짜**: 캘린더 상에 테두리/배경 강조

#### Section 2: 최근 등록된 일정
- **노출 조건**: `createdAt`이 최근 **7일 이내** AND `status == "open"` AND `startAt > 현재시각`
- 마감(신청 완료)된 일정도 포함하여 표시 ("NEW" 배지 부여)
- 신청 가능한 일정 조건도 만족하는 경우 Section 3에도 중복 노출 허용
- 조건에 맞는 일정이 0개이면 섹션 영역 자체를 숨김

#### Section 3: 신청 가능한 일정
- **노출 조건**: `status == "open"` AND `startAt > 현재시각` AND `appliedCount < requiredCount`
- 날짜 오름차순(가장 가까운 일정 순) 정렬
- 항목 정보: 날짜/시간, 학교명, 교육내용, 현재 신청 현황(`1/3명`)
- **빈 상태(Empty State)**: 조건 만족 일정이 없으면 `"아직 신청 가능한 일정이 없어요 🌱"` 안내 메시지 표시

---

### 4.3 일정 상세 화면 (`/schedule/:id`)

#### 상태 배지 판단 기준 (우선순위 순서대로 1개만 부여)
| 우선순위 | 조건 | 배지 명칭 | 스타일 |
|---------|------|----------|--------|
| 1 | `status == "cancelled"` | **취소됨** | 회색 배지 |
| 2 | `status == "confirmed"` | **확정완료** | 파란색 배지 |
| 3 | `endAt < 현재시각` | **종료** | 회색 배지 |
| 4 | `startAt <= 현재시각 < endAt` | **진행중** | 보라색 배지 |
| 5 | `appliedCount >= requiredCount` | **마감** | 주황색 배지 |
| 6 | 그 외 (`open` & 자리있음) | **신청가능** | 초록색 배지 |

#### 표시 정보
- **상단 정보**: 학교명, 교육내용, 날짜/시간, 반 정보(있을 경우), 모집 인원(`n/n명`), 관리자 메모
- **카카오맵**: 학교 위치 지도 표시
  - 좌표(`latitude`, `longitude`) 등록 시 해당 위치 마커 표시
  - SDK 로드 실패 또는 좌표 미등록 시 주소 텍스트 및 "카카오맵에서 보기" 링크 대체
- **참여자 명단**: 현재 신청한 자원봉사자 이름 목록 (실시간 반영)
  - 취소된 일정이라도 취소 전 신청했던 명단은 그대로 확인 가능

#### 하단 액션 버튼 영역

**1) 자원봉사자 접속 시:**
- **신청하기 (초록 버튼)**: 본인 미신청 상태이며, `status == "open"` AND `appliedCount < requiredCount` AND `startAt > 현재시각`
- **신청 취소하기 (회색 버튼)**: 본인이 이미 신청한 상태이며, `status != "confirmed"` AND `status != "cancelled"` AND `startAt > 현재시각`
  - *(중요: 다른 사람으로 인해 정원이 꽉 찬 '마감' 상태여도, 관리자 확정 전이라면 본인 신청 취소 가능)*
- **비활성화 안내**: 마감(정원 충족으로 타인 신청 불가), 확정완료, 취소됨 상태일 때 사유 안내 문구 표시
- **종료된 일정** (`endAt < 현재시각`): 하단 버튼 영역 미노출

**2) 관리자 접속 시:**
- 일반 신청 버튼 대신 **[수정하기]** 및 **[일정 취소]** 관리자 전용 버튼 노출

---

### 4.4 참여 신청 및 취소 로직

#### 신청 로직 (Firestore Transaction)
1. 자원봉사자가 "참여 신청하기" 클릭
2. Transaction 실행:
   - `/volunteers/{volunteerId}` 문서가 존재하는지 검증
   - 해당 `schedules/{scheduleId}`의 `status == "open"`, `startAt > now()`, `appliedCount < requiredCount` 검증
   - 이미 `{scheduleId}_{volunteerId}` 문서가 존재하는지 중복 검증
3. 검증 성공 시:
   - `responses/{scheduleId}_{volunteerId}` 문서 생성
   - `schedules.appliedCount` 1 증가
4. Transaction 실패 시 사유별 안내:
   - 정원 초과: `"마감되었습니다."`
   - 등록된 자원봉사자 아님: `"등록된 자원봉사자만 신청할 수 있습니다."`
   - 이미 신청함: `"이미 신청한 일정입니다."`

#### 취소 로직 (Firestore Transaction)
1. 자원봉사자가 "신청 취소하기" 클릭 → 확인 모달 표시
2. Transaction 실행:
   - 해당 `schedules/{scheduleId}`의 `status == "open"` 및 `startAt > now()` 검증
   - `responses/{scheduleId}_{volunteerId}` 문서가 존재하는지 검증
3. 검증 성공 시:
   - `responses/{scheduleId}_{volunteerId}` 문서 삭제
   - `schedules.appliedCount` 1 감소
4. 신청 문서가 없으면 `"신청 내역이 없습니다."` 메시지 표시 후 최신 데이터로 화면 갱신

---

### 4.5 내 일정 화면 (`/my-schedules`)

- 현재 선택된 `volunteerId`로 신청된 모든 일정 목록을 모아서 표시
- **정렬 순서**:
  1. **진행중 일정** (`startAt <= 현재시각 < endAt`) 최상단
  2. **다가오는 일정** (`startAt > 현재시각`) 날짜 오름차순 (가까운 날짜 먼저)
  3. **종료된 지난 일정** (`endAt <= 현재시각`) 날짜 내림차순 (최근 지난 날짜 먼저)
- **표시 스타일**:
  - 다가오는 일정 / 진행중 일정: 컬러 및 상태 배지 표시
  - 종료된 일정 / 취소된 일정: 그레이스케일(흑백 톤) 처리
- **빈 상태(Empty State)**: 신청한 일정이 없으면 `"신청한 일정이 없어요. 일정을 확인하고 신청해보세요! 📅"` 안내 표시

---

### 4.6 관리자 기능

#### 4.6.1 관리자 대시보드 (`/admin`)
- **네비게이션 탭/버튼**: [일정 등록], [봉사자 명단 관리], [교육내용 관리]
- **일정 목록 필터**: `전체` / `신청가능` / `마감` / `확정완료` / `취소됨` / `종료`
- **일정 카드 UI (아코디언 방식)**:
  - 기본 카드: 날짜/시간, 학교명, 교육내용, 신청현황(`n/n명`), 상태 배지, [확정하기] 버튼, [취소] 버튼
  - **카드 클릭 시**: 페이지 이동 없이 카드 아래로 **아코디언 형태로 펼쳐짐**
    - 현재 신청자 명단 목록 및 각 이름 옆 `[제거(X)]` 버튼
    - 등록된 자원봉사자 중 선택하여 직접 추가할 수 있는 `[참여자 수동 추가]` 드롭다운
    - [일정 정보 수정] 페이지 이동 버튼

#### 4.6.2 참여 확정 처리
- **동작**: 일정 카드의 `[확정하기]` 클릭 시 `status = "confirmed"`로 변경
- **조건**: `status == "open"` 상태이면 **모집 인원 충족 여부와 관계없이 관리자 판단으로 언제든 확정 가능**
- **확정 후**:
  - 자원봉사자의 자발적 신청 및 취소 전면 차단
  - 관리자 대시보드에서만 참여자 명단 추가/제거 가능
  - 일정 기본 정보 수정은 계속 가능

#### 4.6.3 참여자 명단 직접 조정 (관리자)
- 위치: 관리자 대시보드 일정 카드 펼침(아코디언) 영역
- **참여자 추가**: 등록된 자원봉사자 목록에서 선택 → `responses` 문서 생성 및 `appliedCount` +1
  - 이미 신청된 봉사자는 선택 불가
  - 이미 참여자 명단에 있는 봉사자를 추가하려 하면 `"이미 참여자 명단에 있습니다."` 메시지 표시
  - 관리자는 정원을 초과하여 강제 추가 가능 (예: `4/3명` 정상 표기)
- **참여자 제거**: 명단에서 `X` 클릭 → `responses` 문서 삭제 및 `appliedCount` -1
- 참여자 추가/제거 시 `schedules.updatedAt` 갱신

#### 4.6.4 일정 등록 (`/admin/schedule/new`)
- **입력 필드**:
  - 학교명 (필수, 텍스트)
  - 주소 및 카카오맵 검색 (필수, 학교명 검색 결과 선택 시 주소/위도/경도 자동 입력, 수동 입력 가능)
  - 교육 내용 (필수, 관리자가 등록한 과목 드롭다운 선택)
  - 날짜 (필수, 오늘 날짜 포함 가능)
  - 시작 시간 / 종료 시간 (필수)
  - 모집 인원 (필수, 1명 이상)
  - 반 정보 (선택, 예: 3반)
  - 메모 (선택)
- **입력값 유효성 검증**:
  - `startAt <= 현재시각` 입력 차단
  - `종료 시간 <= 시작 시간` 입력 차단
  - `모집 인원 <= 0` 입력 차단
- 저장 시 `status: "open"`, `appliedCount: 0`, `createdAt: now()`, `updatedAt: now()` 생성

#### 4.6.5 일정 수정 (`/admin/schedule/:id/edit`)
- 확정/취소 여부와 무관하게 언제든 일정 정보 수정 가능
- **수정 저장 시 최신 데이터로 덮어쓰기 저장 (수정 이력 기능은 MVP 제외)**
- 수정 저장 시 `updatedAt: now()` 갱신
- 수정 완료 시 일정 상세 및 메인 화면에 최신 정보가 즉시 반영됨

#### 4.6.6 일정 취소 (삭제)
- 관리자 대시보드 또는 상세 화면에서 `[일정 취소]` 클릭 → 확인 모달 후 `status: "cancelled"`, `updatedAt: now()`로 변경
- 실제 문서를 삭제하지 않고 상태만 변경하여 기존 신청 데이터 보존
- 실수 취소 복원 기능은 지원하지 않음 (필요 시 신규 일정 등록)

#### 4.6.7 자원봉사자 명단 관리 (`/admin/volunteers`)
- 자원봉사자 이름 추가 및 삭제
- 삭제 시 기존 `responses` 신청 기록의 이름 스냅샷은 그대로 보존
- 삭제 후 같은 이름을 다시 등록하면 새 `volunteerId`가 생성되며, 과거 신청 내역과 연결하지 않음
- 빈 상태: `"등록된 자원봉사자가 없습니다. 이름을 추가해주세요."`

#### 4.6.8 교육내용 과목 관리 (`/admin/subjects`)
- 교육 과목(예: 도박예방, 약물예방 등) 추가 및 삭제
- 삭제하더라도 기존에 이미 등록된 일정의 교육내용 텍스트는 보존
- 빈 상태: `"등록된 교육내용이 없습니다. 항목을 추가해야 일정을 등록할 수 있습니다."`

---

## 5. 비기능 요구사항

| 항목 | 요구사항 |
|------|---------|
| 반응형 디자인 | 모바일 퍼스트 (스마트폰 375px~430px 너비 최적화 UI, PC 환경도 깔끔한 중앙 카드 뷰) |
| 실시간 동기화 | Firestore `onSnapshot` 리스너를 활용하여 신청/마감 현황 새로고침 없이 즉각 반영 |
| 시간대 기준 | 모든 일시 처리 및 비교는 **Asia/Seoul (KST)** 기준 |
| 지난 일정 처리 | `endAt < 현재시각` 경과 즉시 화면상 그레이스케일 처리 |
| 성능 | 모바일 3G/LTE 환경에서 초기 렌더링 3초 이내 |
| 브라우저 호환성 | 모바일 Safari (iOS), 모바일 Chrome (Android) 완벽 지원 |

---

## 6. 디자인 가이드

| 항목 | 방향 |
|------|------|
| 톤앤매너 | 따뜻하고 친근하며 신뢰감을 주는 파스텔 톤 |
| 주 색상 (Primary) | 따뜻한 초록 계열 (`#4CAF50`, `#81C784`, `#E8F5E9`) |
| 보조 색상 (Secondary) | 부드러운 아이보리, 연한 주황, 연두 |
| 폰트 | Noto Sans KR (가독성 높은 한글 고딕 폰트) |
| 모서리 라운드 | 둥글고 부드러운 스타일 (`border-radius: 12px ~ 16px`) |
| 지난/취소 일정 | CSS `filter: grayscale(100%)` 및 불투명도 조절 (`opacity: 0.7`) |

---

## 7. 데이터 모델 (Firestore)

### 1) `/volunteers/{volunteerId}`
```typescript
{
  name: string;          // 자원봉사자 이름
  createdAt: Timestamp;  // 등록 일시
}
```

### 2) `/subjects/{subjectId}`
```typescript
{
  name: string;          // 교육 과목명 (예: "도박예방")
  createdAt: Timestamp;  // 등록 일시
}
```

### 3) `/schedules/{scheduleId}`
```typescript
{
  schoolName: string;        // 학교명 (예: "봉림초등학교")
  subject: string;           // 교육 내용 (예: "도박예방")
  startAt: Timestamp;        // 시작 일시 (KST)
  endAt: Timestamp;          // 종료 일시 (KST)
  address: string;           // 학교 도로명/지번 주소
  latitude?: number;         // 위도 (카카오맵 좌표)
  longitude?: number;        // 경도 (카카오맵 좌표)
  requiredCount: number;     // 모집 정원
  appliedCount: number;      // 현재 신청 완료 인원 수 (Transaction 관리)
  classInfo?: string;        // 반 정보 (예: "반별진행-3반")
  note?: string;             // 관리자 메모
  status: "open" | "confirmed" | "cancelled";  // 일정 상태
  createdAt: Timestamp;      // 등록 일시
  updatedAt: Timestamp;      // 마지막 수정/상태 변경/명단 조정 일시
}
```

### 4) `/responses/{scheduleId_volunteerId}`
> 💡 Document ID 규칙: `{scheduleId}_{volunteerId}` (동일 일정 중복 신청 원천 방지)
```typescript
{
  scheduleId: string;        // 일정 ID
  volunteerId: string;       // 자원봉사자 Document ID
  volunteerName: string;     // 신청 당시 봉사자 이름 (스냅샷)
  createdAt: Timestamp;      // 신청 일시
}
```

---

## 8. Firestore 보안 규칙 (Rules)

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 관리자 여부 확인 함수 (지정된 2개 이메일 인증 확인)
    function isAdmin() {
      return request.auth != null && 
        (request.auth.token.email == "admin1@dodam.com" || request.auth.token.email == "admin2@dodam.com");
    }

    // 일정 컬렉션
    match /schedules/{scheduleId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if false; // 실제 삭제 방지 (cancelled 상태 업데이트로 처리)
    }

    // 봉사자 명단 컬렉션
    match /volunteers/{volunteerId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // 교육 과목 컬렉션
    match /subjects/{subjectId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // 신청 응답 컬렉션
    match /responses/{responseDocId} {
      allow read: if true;
      // 봉사자 신청/취소 허용 (클라이언트 Transaction에서 일정 상태 및 정원 검증)
      allow create, delete: if true;
      allow update: if isAdmin();
    }
  }
}
```

---

## 9. 기술 스택

| 분류 | 기술 |
|------|------|
| 프론트엔드 프레임워크 | Vue.js 3 (Composition API, `<script setup>`) |
| 빌드 도구 | Vite |
| 상태 관리 | Pinia |
| 라우팅 | Vue Router 4 |
| 스타일링 | Tailwind CSS |
| 백엔드 / 데이터베이스 | Firebase Firestore (실시간 리스너 및 Transaction) |
| 관리자 인증 | Firebase Authentication (이메일/비밀번호) |
| 지도 연동 | 카카오맵 Javascript SDK (장소 검색 및 지도 표시) |
| 배포 및 호스팅 | Firebase Hosting |

---

## 10. 개발 로드맵 (MVP 단계별 구현)

- [ ] **Phase 1: 프로젝트 셋업 및 인프라 구축**
  - Vue 3 + Vite + Tailwind CSS + Pinia + Vue Router 초기화
  - Firebase SDK 연동 및 Firestore 보안 규칙 설정
- [ ] **Phase 2: 진입 화면 & 관리자 인증**
  - 자원봉사자 이름 검색 드롭다운 입장 및 `localStorage` 자동 로그인
  - 관리자 이메일 로그인 모달 및 세션 관리
- [ ] **Phase 3: 메인 화면 (캘린더 및 목록)**
  - 월별 캘린더 그리드 컴포넌트 (`n/n명` 칩, `+n개 더` 모달, 흑백 필터)
  - 최근 등록된 일정 (7일 이내) 섹션 및 신청 가능 일정 섹션 (빈 상태 대응)
- [ ] **Phase 4: 일정 상세 화면 & 카카오맵**
  - 일정 상세 정보 및 카카오맵 마커 표시
  - 상태 배지 우선순위 렌더링 및 실시간 참여자 명단 표시
- [ ] **Phase 5: 신청/취소 동시성 처리**
  - Firestore Transaction 기반 참여 신청 및 취소 로직 구현
  - 마감 상태 및 확정 상태 버튼 예외 처리
- [ ] **Phase 6: 내 일정 화면**
  - 본인 신청 일정 분류 (진행중, 다가오는, 종료된 일정 정렬 및 흑백 렌더링)
- [ ] **Phase 7: 관리자 일정 관리 (등록/수정/취소)**
  - 카카오맵 키워드 검색 연동 일정 등록 폼 (유효성 검사 적용)
  - 일정 정보 수정 폼 및 일정 취소 모달
- [ ] **Phase 8: 관리자 대시보드 & 과목/봉사자 관리**
  - 아코디언 방식의 참여자 명단 직접 추가/제거 및 확정 처리 기능
  - 자원봉사자 명단(`/admin/volunteers`) 및 교육과목(`/admin/subjects`) CRUD
- [ ] **Phase 9: 최종 QA 및 배포**
  - 모바일 반응형 크로스 브라우징 테스트
  - Firebase Hosting 프로덕션 배포

---

## 11. MVP 제외 기능 (Out of Scope)

| 항목 | 제외 사유 |
|------|----------|
| Web Push / SMS 알림 | 외부 통신망 연동 및 비용 발생, 2차 고도화 시 고려 |
| CSV / 엑셀 / PDF 명단 출력 | 화면상 실시간 대시보드 조회로 충분함 |
| 수정 이력(취소선 히스토리) 저장 | 인터뷰 결과 최신 정보만 깔끔히 노출하는 것으로 합의 |
| 취소 일정 되돌리기(복원) | 관리자 오작동 방지 및 신규 등록으로 대체 가능 |
| 자원봉사자 댓글 / 게시판 | 카카오 단톡방 병행 운영으로 충분 |
| 일정 반복 등록 규칙 | MVP 범위 초과 (개별 등록으로 대응) |
