# PRD: 도담도담 자원봉사 일정관리 시스템

> **문서 버전**: v2.0 (실운영 고도화 및 최신 기능 반영본)  
> **작성일**: 2026-08-28  
> **작성자**: PM / Tech Lead  
> **개발 범위**: Full-Stack Production Release (Vue 3 + Tailwind CSS + Firebase)

---

## 1. 프로젝트 개요

### 1.1 배경 및 목적
기존 네이버 밴드/카카오톡 단톡방 공지 및 댓글 방식으로 발생하던 자원봉사 일정 조율의 한계(공지 묻힘, 시간대 겹침 신청, 실시간 참여 현황 파악 불가)를 해소하고, 관리자와 자원봉사자 모두가 스마트폰과 PC에서 직관적으로 일정을 관리할 수 있는 **모바일 퍼스트 반응형 웹 서비스**를 제공합니다.

### 1.2 서비스명
```
도담도담 자원봉사 일정관리
```

---

## 2. 사용자 역할 및 권한 정의

### 2.1 관리자 (Admin)
| 항목 | 내용 |
|------|------|
| 허용 계정 | `cwacc@hanmail.net`, `admin2@dodam.com` (2명 고정 기준) |
| 인증 방식 | Firebase Authentication (Email/Password) + Firestore Security Rules 화이트리스트 |
| 주요 기능 | 일정 등록/수정/취소(0초 즉시 반영), 자원봉사자 명단 관리, 교육과목 관리, 참여자 수동 추가/제거, 일정 확정 토글 |
| 사용 모드 | 관리자 로그인 시 봉사자 세션과 완전 분리된 독립 관리자 모드로 동작 |

### 2.2 자원봉사자 (Volunteer)
| 항목 | 내용 |
|------|------|
| 인원 | 정예 자원봉사자 15인 실명 (장보윤, 신예은, 정두라, 황초희, 오지원, 천민지, 손민기, 방민서, 최현영, 김규민, 강명진, 김민준, 이서연, 박도현, 배성민 등) |
| 접속 방식 | 최초 분기 화면(`/`)에서 이름 검색/선택 후 1초 입장 (비밀번호 불필요) |
| 주요 기능 | 월별 캘린더 일정 확인, 상세 정보 및 지도 조회, 참여 신청 및 취소(0초 즉시 반영), 내 일정 모아보기 |
| 보호 규칙 | **동일 날짜/시간대 중복 신청 원천 차단**, 상단 봉사자 이름 고정(수정 불가) |

---

## 3. UI/UX 및 네비게이션 구조

### 3.1 상단 헤더 (`AppHeader.vue`)
- **도담도담 로고**: 클릭 시 언제든지 **최초 진입 화면(`/`)**으로 이동하여 봉사자를 재선택하거나 관리자 로그인으로 전환 가능.
- **뒤로가기 (`←`)**: 메인 및 대시보드를 제외한 하위 화면에서 이전 페이지 복귀 지원.
- **우측 배지 (역할 분리)**:
  - 관리자: `[관리자]` 배지 + `로그아웃` 버튼 노출.
  - 봉사자: 현재 접속한 `[봉사자 이름]` 고정 배지 노출 (실수 수정 방지).

### 3.2 하단 네비게이션 바 (`AppBottomNav.vue`)
- **[홈]**: 메인 캘린더 및 추천 일정 화면 (`/main`)
- **[내 일정 / 전체일정]**: 봉사자는 본인 신청 일정 (`/my-schedules`), 관리자는 전체 관리 일정 (`/admin`)
- **[관리자]**: 관리자 대시보드 (`/admin`) 및 빠른 로그인 모달 연결

---

## 4. 핵심 기능 요구사항

### 4.1 ⚡ 초고속 Optimistic UI (낙관적 즉시 처리)
- **신청 / 취소**: 버튼 클릭 시 서버 통신 대기(스피너 멈춤) 없이 **0.01초 만에 즉시 상태 및 인원수 갱신**.
- **봉사자 / 교육과목 추가 & 삭제**: 등록/삭제 즉시 목록에 즉각 반영되고 백그라운드에서 동기화.
- **관리자 일정 취소**: 대기 시간 없이 즉시 '취소됨' 상태로 전환.

### 4.2 ⏱️ 동일 날짜/시간대 중복 신청 방지 (Conflict Prevention)
- 봉사자가 새로운 일정에 신청할 때, 이미 신청한 다른 일정들과 **시작~종료 시간(Time Window) 겹침을 실시간 정밀 검사**.
- 시간이 겹치는 경우 `"이미 같은 시간대에 신청된 일정(학교명)이 있어 신청할 수 없습니다."` 안내 및 신청 차단.

### 4.3 📅 캘린더 및 학교명 스마트 축약
- **스마트 3~4자 축약 (`formatShortSchoolName`)**:
  - `창원 봉림초등학교` ➔ `봉림초`
  - `창원꽃무지풀무지유치원` ➔ `꽃무지유`
  - `감계초등학교 병설유치원` ➔ `감계유`
- **취소된 일정 렌더링**: '취소' 텍스트 접두사 없이 **은은한 그레이톤(Grayscale & Opacity 60%)**으로 깔끔하게 처리. (상세 화면에서 '취소됨' 명확 표시)

### 4.4 🗺️ 무설정 인터랙티브 지도 엔진 (`KakaoMap.vue`)
- Kakao Maps API 키가 없거나 외부망 차단 환경에서도 **OpenStreetMap + Leaflet 인터랙티브 맵**이 즉시 구동.
- 줌, 팬, 마커, 인포윈도우 완벽 지원 및 '카카오맵 길찾기' 바로가기 연동.
- 일정 등록/수정 시 경남/창원 학교명 입력 시 도로명 주소 및 좌표 실시간 자동완성(Auto-Fill).

---

## 5. 데이터 구조 및 영속성 보장

### 5.1 Single Source of Truth 3중 보존 체계
1. **1차**: In-Memory Reactive Cache (0초 렌더링)
2. **2차**: Browser LocalStorage 영구 보존 (`dodam_custom_schedules`, `dodam_custom_volunteers`, `dodam_custom_subjects`, `dodam_custom_responses`)
3. **3차**: Firebase Firestore Cloud Database 백그라운드 원자적 동기화

### 5.2 기본 탑재 데이터
- **실제 교육 일정 24건**: 2026년 3월~10월 유치원 12건 + 초등학교 12건
- **실제 자원봉사자 15인 실명 명단**: 장보윤, 신예은, 정두라, 황초희, 오지원, 천민지 등
- **실제 밴드/카톡 신청자 매핑**: 각 일정별 배정 인원 100% 반영
